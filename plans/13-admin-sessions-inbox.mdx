# Admin sessions inbox and archive

Replace the **Show due** / **Show archived** checkbox model with two tabs: **Inbox** (unarchived admin pile) and **All sessions** (every booking, archived or not). Server-side lists per tab. `hiddenAt` stays the archive flag. Editors stop treating archive as “gone.” Auto-archive when a session is fully done, or when `status` becomes a dead checkout/lifecycle value (`cancelled`, `abandoned`, `expired`). Keep actionable failures in inbox (`failed`, `email_failed`).

Open locally: `mdxp plans/13-admin-sessions-inbox.mdx`

<FileTree
	title="Plan scope (high level)"
	entries={[
		{
			path: "convex/schema.ts",
			change: "modified",
			note: "index for inbox pagination; all uses full list"
		},
		{
			path: "convex/services/sessions.ts",
			change: "modified",
			note: "list queries by view; cancel + dead checkout archive"
		},
		{
			path: "convex/services/sessionCheckout.ts",
			change: "modified",
			note: "expired / abandoned → hiddenAt"
		},
		{
			path: "convex/services/packageScheduling.ts",
			change: "modified",
			note: "package session cancel → hiddenAt"
		},
		{
			path: "convex/lib/editorSessions.ts",
			change: "modified",
			note: "drop hiddenAt from editor visibility"
		},
		{
			path: "convex/lib/editorAssignments.ts",
			change: "modified",
			note: "assign on archived OK if confirmed"
		},
		{
			path: "convex/lib/adminSessionList.ts",
			change: "added",
			note: "inbox pagination + includeStale filter (stage 2)"
		},
		{
			path: "convex/lib/sessionArchive.ts",
			change: "added",
			note: "archiveDeadCheckoutBooking, archiveSessionWhenFullyDone, unarchiveSessionForNewUnpaidInvoice"
		},
		{
			path: "convex/services/stripeInvoices.ts",
			change: "modified",
			note: "booking invoice record unarchive; mark paid → archiveSessionWhenFullyDone"
		},
		{
			path: "src/sites/studio/features/admin/components/SessionsListViewTabs.tsx",
			change: "added",
			note: "Inbox / All sessions tabs (stage 2)"
		},
		{
			path: "src/sites/studio/features/admin/components/SessionsTable.tsx",
			change: "modified",
			note: "tabs; list args follow inbox vs all"
		},
		{
			path: "src/sites/studio/features/admin/components/SessionsTableFilters.tsx",
			change: "modified",
			note: "remove Show due / Show archived; keep Show unconfirmed"
		},
		{
			path: "src/sites/studio/features/admin/lib/admin-sessions.ts",
			change: "modified",
			note: "drop client inbox rules; optional due badges only"
		},
		{
			path: "src/sites/studio/features/admin/components/SessionActionsMenu.tsx",
			change: "modified",
			note: "archive confirm + copy (stage 4)"
		}
	]}
/>

<Callout
	tone="decision"
	body={`**Decisions from design review**

- **Inbox** = bookings where \`hiddenAt\` is unset (server query only). Includes actionable problems: \`failed\` (paid but confirmation broke: slot conflict, calendar create, invalid input) and \`email_failed\` (confirmed, receipt email failed).
- **All sessions** = every booking, archived and unarchived. Archive is filing state (`hiddenAt`), not a separate booking status. Stage 2 does not add an archived badge; use **All sessions** tab or manual archive actions to see filed rows.
- **Archive** = set \`hiddenAt\` (drops off inbox, still visible on **All sessions**). **Unarchive** = clear \`hiddenAt\` (back to inbox). Schema unchanged.
- **Manual archive** stays; rare with auto-archive. Stage 4 may add a confirm when deliverables are not **Sent** and an editor is assigned.
- **Auto-archive (done)** when \`confirmed\` or \`email_failed\`, past, no unpaid Stripe invoices, deliverables **Sent** (\`editStatus === "completed"\`). Event-driven only; no cron. \`email_failed\` is eligible for this path once work is truly finished (same as confirmed).
- **Auto-archive (dead status)** when \`status\` becomes \`cancelled\`, \`abandoned\`, or \`expired\` — same patch sets \`hiddenAt\` via one helper. Includes admin **Cancel booking** and checkout cleanup. **Not** \`failed\` or \`email_failed\`.
- **Do not** auto-archive live \`pending_payment\` until \`expired\` / \`abandoned\` (or admin cancels).
- **Auto-unarchive** when a new unpaid invoice is attached to an archived booking, only if \`status\` is \`confirmed\` or \`email_failed\` (never for \`cancelled\` / \`expired\` / \`abandoned\` / \`failed\`).
- **Editors**: \`hiddenAt\` does not hide sessions or block deliverables. \`cancelled\` and non-confirmed statuses still block editors as today.`}
  />

---

## Mental model

<Mermaid
	caption="Admin vs editor"
	source={`flowchart TB
  subgraph admin["Admin dashboard"]
    I[Inbox tab\nhiddenAt unset]
    A[All sessions tab\nevery booking]
    I -->|archive sets hiddenAt| F[Filed row still on All sessions]
    F --> A
    I -->|unarchive or new unpaid invoice| I
    I -->|cancelled / expired / abandoned| F
  end
  subgraph editor["Editor dashboard"]
    E[Assigned confirmed sessions]
  end
  I -.->|archive does not affect| E
  I -->|cancel only| X[Editor loses access]`}
/>

<Callout
	tone="info"
	body="**Show due** logic (upcoming OR unpaid OR deliverables not sent) no longer defines which tab a row is on. Reuse it for sort order, badges, or row emphasis inside **Inbox** only if useful."
/>

---

## UI target

<Tabs>
	<Tab
		id="inbox"
		label="Inbox tab">
		<Code
			language="text"
			caption="Default admin sessions view"
			code={`┌──────────────────────────────────────────────┐
│ Sessions                                     │
│ Search…     [ Inbox * ] [ All sessions ]       │
│              ☑ Show unconfirmed   (Filters ▾)  │
├──────────────────────────────────────────────┤
│  … unarchived: confirmed, email_failed,      │
│    failed, pending_payment, …                │
│  ⋮ Archive session  |  Cancel booking        │
└──────────────────────────────────────────────┘`}
		/>
	</Tab>
	<Tab
		id="all"
		label="All sessions tab">
		<Code
			language="text"
			caption="Full history"
			code={`┌──────────────────────────────────────────────┐
│ Sessions                                     │
│ Search…     [ Inbox ] [ All sessions * ]       │
│              ☑ Show unconfirmed   (Filters ▾)  │
├──────────────────────────────────────────────┤
│  … all bookings (archived + unarchived)      │
│  ⋮ Archive / Unarchive on row              │
└──────────────────────────────────────────────┘`}
		/>
	</Tab>
</Tabs>

---

## Current vs target (data loading)

|              | Before stage 2                                     | Shipped (stage 2)                                                                                            |
| ------------ | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| List API     | Single `listSessions` paginates all bookings       | `listSessions` takes `view: "inbox" \| "all"` and `includeStale`                                             |
| Inbox filter | Client: `filterAdminSessions` + **Show due**       | Server: `hiddenAt` unset via compound index; `includeStale` drops stale checkout rows                        |
| All sessions | Hidden unless **Show archived** on                 | Server: no `hiddenAt` filter; same row styling as inbox (no archived badge)                                  |
| Search       | Client filter on loaded page                       | Still client-only on loaded page (`filterAdminSessions` + `customerFilter`); no server `searchQuery`         |
| Pagination   | One `usePaginatedQuery`; args change with tab/sort | One `usePaginatedQuery` in `AdminDashboard`; args `{ view, includeStale, sort… }`                            |
| Archive      | Hides from editors (fixed stage 1)                 | Admin filing only; row remains queryable on **All sessions**                                                 |
| Dead status  | `cancelled` / `expired` / etc. may stay unarchived | **Shipped (stage 3):** `archiveDeadCheckoutBooking` sets `hiddenAt` on `cancelled` / `expired` / `abandoned` |
| Fully done   | Manual archive only                                | **Shipped (stage 3):** `archiveSessionWhenFullyDone` after **Sent** or booking invoice paid                  |

---

## Stages

Four PR-sized chunks. Order matters for editor decoupling before admins rely on archive-heavy inbox.

| Stage | Focus                              | Outcome                                                                          |
| ----- | ---------------------------------- | -------------------------------------------------------------------------------- |
| 1     | Editor decouple + assignment rules | **Done.** Archive no longer affects editor list or deliverables mutations        |
| 2     | Server list views + admin tabs     | **Done.** Inbox/all `listSessions`; removed **Show due** / **Show archived**     |
| 3     | Auto-archive / unarchive           | **Done.** `convex/lib/sessionArchive.ts` + service hooks (see below)             |
| 4     | UX polish                          | **Done.** Archive confirm when editor assigned and deliverables not sent |

---

## Stage 1 — Decouple archive from editors

**Goal:** `hiddenAt` is admin-only. Cancelled status remains the editor off switch.

<Tabs>
	<Tab id="editor-visible" label="editorSessions.ts">
		<Diff
			id="editor-visible"
			summary="Editor list ignores hiddenAt"
			filename="convex/lib/editorSessions.ts"
			language="ts"
			before={`export function isEditorVisibleSession(session: Doc<"bookings">): boolean {
	const hasEligibleStatus = session.status === "confirmed" || session.status === "email_failed";

    return hasEligibleStatus && session.hiddenAt === undefined;

}`}
			after={`export function isEditorVisibleSession(session: Doc<"bookings">): boolean {
// Admin archive (hiddenAt) must not hide work from editors.
return session.status === "confirmed" || session.status === "email_failed";
}`}
		/>
		<Diff
			id="deliverables-eligibility"
			summary="Deliverables mutations ignore hiddenAt"
			filename="convex/lib/editorSessions.ts"
			language="ts"
			before={` if (session.hiddenAt !== undefined) {
return err({ reason: "SESSION_ARCHIVED" as const });
}

    if (session.sessionStartAt >= Date.now()) {`}
    		after={`	// SESSION_ARCHIVED removed: archive is admin inbox only.

    if (session.sessionStartAt >= Date.now()) {`}
    	/>
    </Tab>
    <Tab id="assign" label="editorAssignments.ts">
    	<Diff
    		id="assign-archived"
    		summary="Allow assign when archived but confirmed"
    		filename="convex/lib/editorAssignments.ts"
    		language="ts"
    		before={`// today: isEditorVisibleSession gates new assignment`}
    		after={`// gate on confirmed | email_failed only, not hiddenAt

// update admin toast copy in SessionEditorAssignment`}
/>

</Tab>

</Tabs>

<Callout
	tone="risk"
	body="Search for \`SESSION_ARCHIVED\`, \`hiddenAt\`, and \`isEditorVisibleSession\` in tests (\`convex/lib/tests/editorSessions.test.ts\`, \`convex/tests/authorization.test.ts\`, editor dashboard tests). Update expectations: archived + confirmed still visible to assigned editor."
/>

<Callout
	tone="info"
	body="**Stage 1 shipped.** Do not reintroduce \`SESSION_ARCHIVED\` or \`hiddenAt\` checks on editor visibility or deliverables eligibility."
/>

---

## Stage 2 — Server inbox / all lists + admin UI (shipped)

**Goal:** Inbox vs **All sessions** tabs drive server-side `listSessions`. Remove **Show due** and **Show archived**. Keep **Show unconfirmed** (`includeStale`).

<Callout
	tone="info"
	body="Implemented on branch \`convert-admin-sessions-inbox-archive-flow\`. Future agents: treat this section as reference, not a todo list."
/>

### Schema index (shipped: Option A)

Compound indexes on `bookings`:

- `by_hiddenAt_and_sessionStartAt` — inbox pagination when sorting by session date
- `by_hiddenAt_and_pendingPaymentCreatedAt` — inbox pagination when sorting by createdAt

Inbox queries use `.withIndex(..., (q) => q.eq("hiddenAt", undefined))`. **All sessions** keeps existing `by_sessionStartAt` / `by_pendingPaymentCreatedAt` with no `hiddenAt` filter.

Option B (denormalized `adminInboxVisible`) was not used.

### List API (shipped)

- **`convex/lib/adminSessionList.ts`**: `paginateAdminSessionsBySessionStart`, `paginateAdminSessionsByCreatedAt`, `passesAdminInboxStaleFilter` (stale = expired / abandoned / checkout past 24h; `cancelled` always excluded when `includeStale` is false).
- **`listSessions` args**: `{ paginationOpts, sortBy, sortDirection, view?: "inbox" | "all", includeStale?: boolean }` (defaults: `view: "inbox"`, `includeStale: true`).
- **`includeStale` applies to both views**, not inbox-only.
- Tests: `convex/tests/adminSessionsList.test.ts` (inbox omits archived; all includes archived).

### Admin UI (shipped)

- **`AdminDashboard`**: one `usePaginatedQuery(api.sessions.listSessions, { view, includeStale, sort… })` (not two hook instances; args change when tab or stale toggle changes).
- **`SessionsListViewTabs`**: Inbox / All sessions beside search; input-matched border/background; full-width tab bar on mobile.
- **`SessionsTableFilters`**: **Show unconfirmed** on desktop and in mobile Filters menu for **both** tabs.
- **`filterAdminSessions`**: client search only (`customerFilter` on loaded pages). No server `searchQuery`. Prefetch in `SessionsTable` when search hides all loaded rows.
- **Preferences**: `sessionsView` + `showStaleBookings`; removed `showArchived` and `showUpcomingOnly`.
- **Row UI**: no archived badge; past sessions keep existing muted styling only.

### Not in stage 2 (added in stage 3)

- Auto-archive on deliverables **Sent** via `updateSessionEditStatusService` → `archiveSessionWhenFullyDone`.
- Dead checkout writes via `archiveDeadCheckoutBooking` (`cancelled` / `expired` / `abandoned`).

### Historical sketches (pre-ship)

<Tabs>
	<Tab
		id="idx-a"
		label="Option A — compound index (chosen)">
		<Code
			language="ts"
			filename="convex/schema.ts (sketch)"
			code={`.index("by_hiddenAt_and_sessionStartAt", ["hiddenAt", "sessionStartAt"])
// Inbox: .withIndex(..., q => q.eq("hiddenAt", undefined)) — verify Convex optional-field index semantics`}
		/>
	</Tab>
	<Tab
		id="idx-b"
		label="Option B — not used">
		<Code
			language="ts"
			code={`// adminInboxVisible: boolean, true when hiddenAt unset; flip on archive/unarchive
// .index("by_adminInboxVisible_and_sessionStartAt", [...])`}
		/>
	</Tab>
</Tabs>

---

## Stage 3 — Auto-archive and auto-unarchive (shipped)

**Goal:** Inbox shrinks when work finishes or a booking dies, without a cron. One module owns the rules.

<Callout
	tone="info"
	body="**Stage 3 shipped** on \`convert-admin-sessions-inbox-archive-flow\`. Reference only — do not re-wire duplicate archive logic outside \`sessionArchive.ts\` and the hooks listed below."
/>

### `convex/lib/sessionArchive.ts`

- `isDeadCheckoutStatus(status)` — `cancelled`, `expired`, or `abandoned`
- `archiveDeadCheckoutBooking(ctx, bookingId, updates)` — `ctx.db.patch` plus `hiddenAt` when `updates.status` is dead checkout
- `isSessionEligibleForAutoArchive(session, stripeSummary, now?)` — “fully done” predicate (unit-tested)
- `archiveSessionWhenFullyDone(ctx, bookingId, now?)` — loads booking + invoice summary; sets `hiddenAt` when eligible; no-op if missing booking
- `unarchiveSessionForNewUnpaidInvoice(ctx, session)` — clears `hiddenAt` for archived `confirmed` / `email_failed` only

### Service hooks (shipped)

| Hook                                         | Calls                                                                                                              |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| `updateSessionEditStatusService`             | `archiveSessionWhenFullyDone` after `saveSessionEditStatus`                                                        |
| `markSessionCalendarEventDeletedService`     | `archiveDeadCheckoutBooking` (`status: "cancelled"`)                                                               |
| `cancelPackageSessionService`                | `archiveDeadCheckoutBooking` (`status: "cancelled"`)                                                               |
| `markSessionExpiredByStripeSessionIdService` | `archiveDeadCheckoutBooking` (`status: "expired"`)                                                                 |
| `deletePendingSessionService`                | `archiveDeadCheckoutBooking` (`status: "abandoned"`)                                                               |
| `recordBookingStripeInvoiceService`          | `unarchiveSessionForNewUnpaidInvoice` when insert created a new row                                                |
| `markStripeInvoicePaidService`               | `archiveSessionWhenFullyDone` when paid invoice has `bookingId` (Stripe `invoice.paid` webhook uses this mutation) |

Package-only and adjustment invoice record paths do **not** call unarchive; paid archive runs only for booking-linked invoices.

### “Fully done” (server)

Mirror today’s inverted **Show due** check:

- `status` is `confirmed` or `email_failed`
- `sessionStartAt` in the past
- no unpaid Stripe invoices for booking
- `editStatus === "completed"` (**Sent**)

### Dead checkout status (auto-archive on write)

`archiveDeadCheckoutBooking` merges field updates and sets `hiddenAt` when `updates.status` is dead checkout:

| Status      | Write paths (shipped)                                                                        |
| ----------- | -------------------------------------------------------------------------------------------- |
| `cancelled` | `markSessionCalendarEventDeletedService` (admin delete event); `cancelPackageSessionService` |
| `expired`   | `markSessionExpiredByStripeSessionIdService`                                                 |
| `abandoned` | `deletePendingSessionService`                                                                |

**Stay in inbox (no auto-archive on status alone):**

| Status            | Why                                                                                                                                                                 |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `failed`          | Paid or claimed checkout; confirmation failed (`BOOKING_TIME_UNAVAILABLE`, `GOOGLE_CALENDAR_CREATE_FAILED`, `BOOKING_INVALID_INPUT`). Admin fix + `confirmBooking`. |
| `email_failed`    | Confirmed session; invoice/receipt email failed. Retry/resend flows.                                                                                                |
| `confirmed`       | Normal work until “fully done” auto-archive or manual archive.                                                                                                      |
| `pending_payment` | Live checkout until `expired` / `abandoned` or cancel.                                                                                                              |

<Callout
	tone="decision"
	body={`Dead checkout sites use \`archiveDeadCheckoutBooking\`. \`markBookingConfirmationFailed\` sets \`failed\` only — no \`hiddenAt\`.`}
/>

<Tabs>
	<Tab
		id="cancel"
		label="Admin cancel">
		<Diff
			id="cancel-archive"
			summary="Dead checkout patch includes hiddenAt"
			filename="convex/services/sessions.ts"
			language="ts"
			before={`ctx.db.patch(args.bookingId, {
	bookingFailureCode: undefined,
	googleCalendarId: undefined,
	googleEventId: undefined,
	status: "cancelled"
})`}
			after={`archiveDeadCheckoutBooking(ctx, args.bookingId, {
	bookingFailureCode: undefined,
	googleCalendarId: undefined,
	googleEventId: undefined,
	status: "cancelled"
})`}
		/>
	</Tab>
	<Tab
		id="checkout"
		label="Checkout cleanup">
		<Diff
			id="checkout-dead-status"
			summary="Expired / abandoned archive"
			filename="convex/services/sessionCheckout.ts"
			language="ts"
			before={`.patch(decision.bookingId, { status: "expired" })
// ...
.patch(args.bookingId, { status: "abandoned" })`}
			after={`archiveDeadCheckoutBooking(ctx, decision.bookingId, { status: "expired" })
// ...
archiveDeadCheckoutBooking(ctx, args.bookingId, { status: "abandoned" })`}
		/>
	</Tab>
</Tabs>

`pending_payment` stays in inbox until it becomes `expired` / `abandoned` or an admin cancels. **Show unconfirmed** controls visibility of stale unarchived checkout rows on **both** inbox and all lists (same as stage 2).

### Triggers (done path — editStatus **Sent**)

All UI paths that mark deliverables **Sent** call `api.sessions.updateSessionEditStatus` with `editStatus: "completed"`. Shipped: **`updateSessionEditStatusService`** calls `archiveSessionWhenFullyDone` after `saveSessionEditStatus` (one server hook).

| Caller                   | How **Sent** is set                                                                              |
| ------------------------ | ------------------------------------------------------------------------------------------------ |
| Admin deliverables tabs  | `useStatusActions` → `handleUpdateEditStatus("completed")`                                       |
| Admin options menu       | **Set deliverables status to sent** → same hook (`SessionActionsMenu`)                           |
| Admin deliverables email | `useDeliverablesEmailAction` → `updateSessionEditStatus({ editStatus: "completed" })` after send |
| Editor dashboard         | `EditorDeliverableStatusBadge` → `updateSessionEditStatus`                                       |

Manual **Set deliverables status to sent** uses the same mutation and archive hook as the **Sent** tab and deliverables email.

`invoice.paid` → `markStripeInvoicePaid` → `markStripeInvoicePaidService` → `archiveSessionWhenFullyDone` when the invoice row has a `bookingId`.

<Mermaid
	caption="Event-driven archive"
	source={`flowchart LR
  T1[deliverables tabs] --> U[updateSessionEditStatus completed]
  T2[options Set status to sent] --> U
  T3[deliverables email] --> U
  U --> M[archiveSessionWhenFullyDone]
  P[invoice.paid webhook] --> M
  M -->|eligible| H[set hiddenAt]
  X[dead checkout status patch] --> H
  N[new unpaid invoice] --> R[unarchiveSessionForNewUnpaidInvoice]
  R --> C[clear hiddenAt]`}
/>

<Callout
	tone="warning"
	body="Booking invoice record/paid paths are wired. Dead checkout statuses never auto-unarchive. New unpaid **booking** invoices unarchive; package/adjustment invoice rows are unchanged."
/>

Internal mutation (no `archive:sessions` permission) used by hooks; manual archive keeps `archiveSession` + permission.

---

## Stage 4 — Manual archive UX (shipped)

<Callout
	tone="info"
	body="**Stage 4 shipped** on \`convert-admin-sessions-inbox-archive-flow\`. \`shouldConfirmSessionArchive\` + \`SessionArchiveDialog\`; menu copy **Archive session** / **Unarchive session**."
/>

When admin chooses **Archive session**:

- If `assignedEditorTokenIdentifier` is set and `editStatus !== "completed"`, `SessionArchiveDialog` explains the session stays on the editor dashboard until cancel or deliverables sent.
- Otherwise archive runs immediately (same as before).

Cancel-booking dialog copy notes auto-archive on cancel (stage 3).

Optional backfill for old dead-status rows remains out of scope unless run manually (see below).

---

## Cancel vs archive (reference)

| Action              | Menu label            | Calendar  | `status`                | `hiddenAt`                    | Editors                   |
| ------------------- | --------------------- | --------- | ----------------------- | ----------------------------- | ------------------------- |
| Cancel booking      | **Cancel booking**    | Removed   | `cancelled`             | set                           | No access                 |
| Checkout dies       | (automatic)           | —         | `expired` / `abandoned` | set                           | No access                 |
| Confirmation failed | (automatic)           | —         | `failed`                | unset (stays in inbox)        | No access until confirmed |
| Email failed        | (automatic)           | —         | `email_failed`          | unset until done auto-archive | Access like confirmed     |
| Archive             | **Archive session**   | Unchanged | Unchanged               | set                           | Still access (stage 1)    |
| Unarchive           | **Unarchive session** | Unchanged | Unchanged               | cleared                       | Unchanged                 |

---

## Testing

- **Stage 2 (shipped):** `convex/tests/adminSessionsList.test.ts` — inbox vs all `hiddenAt` filtering.
- **Stage 1 (shipped):** editor list includes archived confirmed session; assign on archived confirmed; deliverables eligibility ignores `hiddenAt`.
- **Stage 3 (shipped):** `convex/lib/tests/sessionArchive.test.ts` (eligibility + `isDeadCheckoutStatus`); `convex/tests/sessionArchive.test.ts` (sent → archive, delete event, `failed` stays visible, unarchive on new booking invoice, pay → archive).
- Authorization: `archive:sessions` / `delete:sessions` unchanged for manual paths.
- Admin E2E (if present): tab switch changes `listSessions` args; **Show unconfirmed** toggles `includeStale` on both tabs.

---

## Out of scope (for now)

- Same tab pattern for **Packages** table (still has **Show due** / archive today).
- Cron sweep for missed auto-archive.
- Migrating old dead-status-but-unarchived rows (optional one-off backfill in stage 4).

<Callout
	tone="info"
	body="Optional backfill: set \`hiddenAt\` on existing \`cancelled\`, \`abandoned\`, or \`expired\` rows with unset \`hiddenAt\`. Leave \`failed\` and \`email_failed\` unarchived unless already filed. **All sessions** lists them either way."
/>
