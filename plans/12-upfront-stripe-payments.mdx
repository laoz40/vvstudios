# Upfront Stripe payments

Move all customer payments to Stripe upfront. No deposits, no bank transfer, no invoice emails. Receipts replace invoices. Stripe Invoicing handles post-booking charges.

Open locally: `mdxp plans/12-upfront-stripe-payments.mdx`

<FileTree
	title="Plan scope (high level)"
	entries={[
		{
			path: "convex/services/stripe.ts",
			change: "modified",
			note: "full-amount checkout, Stripe Customer"
		},
		{
			path: "convex/services/bookingConfirmation*.ts",
			change: "modified",
			note: "receipt instead of invoice"
		},
		{
			path: "convex/services/packagePayment.ts",
			change: "modified",
			note: "checkout instead of invoice-first"
		},
		{
			path: "convex/services/packageAdjustmentInvoices.ts",
			change: "modified",
			note: "Stripe Invoicing"
		},
		{ path: "convex/http.ts", change: "modified", note: "checkout + invoice.paid webhooks" },
		{
			path: "src/sites/studio/features/booking-form/",
			change: "modified",
			note: "deposit copy removed, package checkout"
		},
		{
			path: "src/sites/studio/features/booking-invoice/",
			change: "modified",
			note: "receipt PDF + email"
		},
		{
			path: "src/sites/studio/features/booking-complete/",
			change: "modified",
			note: "unified complete page"
		},
		{
			path: "convex/services/stripeInvoicing.ts",
			change: "added",
			note: "admin ad-hoc Stripe Invoice (booking + package)"
		},
		{
			path: "src/sites/studio/features/admin/components/StripeInvoiceDialog.tsx",
			change: "added",
			note: "multi-line ad-hoc invoice form"
		},
		{
			path: "src/sites/studio/features/admin/",
			change: "modified",
			note: "resend/download receipt, Stripe invoice, legacy read-only invoices"
		},
		{
			path: "convex/schema.ts",
			change: "modified",
			note: "stripeCustomerId; legacy fields kept optional"
		}
	]}
/>

<Callout
  tone="decision"
  body={`**Decisions locked in from design review**

- Full upfront payment via Embedded Checkout (sessions + packages)
- Itemized \`price_data\` line items from \`DURATION_PRICES\` / \`ADDON_PRICES\` — no fixed Stripe Price IDs for deposit or processing fee
- New Stripe Customer per checkout (\`email\` + \`name\` only; no extra customer metadata); \`stripeCustomerId\` stored on booking/package record
- \`stripePaymentIntentId\` stored on booking via webhook; not shown on customer receipt PDF (admin copy UI in stage 4a)
- Stripe Invoicing for adjustments + admin ad-hoc charges (multi-line)
- Scheduling link emailed only (not on complete page)
- Legacy schema fields kept for old bookings; nothing new writes to them
- Admin: remove payment status controls; legacy payment info read-only; keep total price column`}
  />

---

## Current vs target

<Mermaid
	caption="Single session flow"
	source={`flowchart LR
  subgraph now["Now"]
    A1[Form] --> B1["Stripe $50 deposit"]
    B1 --> C1[Confirm + calendar]
    C1 --> D1["Invoice email (balance due)"]
    D1 --> E1[Bank transfer]
    E1 --> F1[Admin marks paid]
  end
  subgraph target["Target"]
    A2[Form] --> B2["Stripe full amount"]
    B2 --> C2[Confirm + calendar]
    C2 --> D2[Receipt email]
  end`}
/>

<Mermaid
	caption="Package flow"
	source={`flowchart LR
  subgraph nowP["Now"]
    P1[Form] --> P2[Invoice email]
    P2 --> P3[Bank transfer]
    P3 --> P4[Admin confirm]
    P4 --> P5[Scheduling email]
  end
  subgraph targetP["Target"]
    Q1[Form] --> Q2["Stripe full amount"]
    Q2 --> Q3[Webhook paid]
    Q3 --> Q4[Scheduling email]
    Q2 --> Q5["/booking-complete"]
  end`}
/>

---

## Stages

Six stacked PRs. Each stage is one branch in the stack; later stages depend on earlier ones. Stage 4 ships as two PRs.

| Stage | Branch                                       | PR focus                                                              | Status                                              |
| ----- | -------------------------------------------- | --------------------------------------------------------------------- | --------------------------------------------------- |
| 1     | `feat/upfront-stripe/session-checkout`       | Single-session full Stripe checkout + receipts                        | Done — manual Stripe QA pending (see verify)        |
| 2     | `feat/upfront-stripe/package-checkout`       | Package Stripe checkout + auto scheduling email                       | Done — E2E still on old flow (stage 5)              |
| 3     | `feat/upfront-stripe/adjustment-invoicing`   | Stripe Invoicing for package adjustments                              | Done                                                |
| 4a    | `feat/upfront-stripe/admin-billing`          | Remove payment controls, resend receipt, copy Stripe Payment ID       | Open — PR #96                                       |
| 4b    | `feat/upfront-stripe/admin-stripe-invoicing` | Ad-hoc Stripe Invoice, receipt PDF download, legacy invoice read-only | In progress — open PR stacked on 4a                 |
| 5     | `feat/upfront-stripe/tests-cleanup`          | Tests, dead-code removal, copy polish (split into parts below)        | In progress — parts 1–4 done; part 5 verify pending |

---

## Stacked PRs (`gh stack`)

Ship each stage as its own PR, stacked on the previous one. Build one branch at a time — no need to create the full stack upfront.

**Stage 1** (from `production`):

<Code
	language="bash"
	code={`git checkout production && git pull
gh stack init feat/upfront-stripe/session-checkout
# implement stage 1, commit, then:
gh stack submit`}
/>

**Next stages** (when the previous stage is done or in review):

<Code
	language="bash"
	code={`gh stack add feat/upfront-stripe/package-checkout   # stage 2; use stage branch from table
# implement that stage only, commit, then:
gh stack submit`}
/>

Repeat with `gh stack add` for stages 3–5 (use `admin-billing` then `admin-stripe-invoicing` for stage 4). Each new branch sits on top of the current stack tip.

<Callout
	tone="info"
	body="One stage per branch per PR. Use the matching stage section below as the PR description. Run `gh stack rebase` if `production` or a lower PR changes."
/>

---

## Stage 1 — Single sessions: full checkout + receipts

**Goal:** Customer pays the full session total in Stripe. Receipt emailed on confirmation. Deposit and processing fee gone.

**Status:** Implemented on `feat/upfront-stripe/session-checkout`. Manual Stripe QA still pending (see verify checklist).

### Backend

<Tabs>
  <Tab id="stripe-checkout" label="Stripe checkout">
    <Diff
      id="stripe-full-amount"
      summary="Charge full session total with itemized price_data"
      filename="convex/services/stripe.ts"
      language="ts"
      before={`// Fixed deposit + processing fee line items
line_items: [
  { price: env.STRIPE_BOOKING_DEPOSIT_PRICE_ID, quantity: 1 },
  { price: env.STRIPE_PROCESSING_FEE_PRICE_ID, quantity: 1 },
]`}
      after={`// Itemized from booking-pricing constants (convex/lib/stripeCheckoutLineItems.ts)
const lineItems = buildSessionCheckoutLineItems({ duration, addons });

// Checkout helpers in convex/lib/stripeCheckoutSession.ts
const customer = await stripe.customers.create({ email, name });

line_items: lineItems,
customer: customer.id,
metadata: { bookingId },
// store stripeCustomerId + stripeSessionId on pending booking`}
/>

  </Tab>
  <Tab id="env-cleanup" label="Env vars">
    <Diff
      id="remove-price-ids"
      summary="Remove deposit and processing fee price IDs"
      filename="convex/env.ts"
      language="ts"
      before={`STRIPE_BOOKING_DEPOSIT_PRICE_ID: z.string(),
STRIPE_PROCESSING_FEE_PRICE_ID: z.string(),`}
      after={`// Removed — prices calculated at runtime from shared constants`}
    />
  </Tab>
  <Tab id="schema-customer" label="Schema">
    <Diff
      id="booking-stripe-customer"
      summary="Add stripeCustomerId to bookings"
      filename="convex/schema.ts"
      language="ts"
      before={`stripeSessionId: v.optional(v.string()),
stripePaymentIntentId: v.optional(v.string()),`}
      after={`stripeSessionId: v.optional(v.string()),
stripePaymentIntentId: v.optional(v.string()),
stripeCustomerId: v.optional(v.string()), // new per checkout`}
    />
  </Tab>
</Tabs>

**New libs (implemented):**

- `convex/lib/stripeCheckoutLineItems.ts` — builds Stripe `price_data` lines from duration, addons, quantities. Single source of truth with frontend pricing.
- `convex/lib/stripeCheckoutSession.ts` — customer create, embedded checkout session, link `stripeCustomerId` / `stripeSessionId` to pending booking.

**Webhook:** `checkout.session.completed` path unchanged structurally. Confirmation still claims → calendar → confirmed. Stores `stripePaymentIntentId` on booking. Swaps invoice send for receipt send.

### Receipt emails (sessions)

<Diff
	id="receipt-send"
	summary="Send receipt instead of invoice on confirmation"
	filename="convex/lib/bookingConfirmation.ts"
	language="ts"
	before={`await sendBookingInvoiceEmailsForBooking(ctx, bookingId);`}
	after={`await sendBookingReceiptEmailsForBooking(ctx, bookingId);
// PDF title: "Receipt", no bank details, "Total paid"`}
/>

| Change        | Detail                                                                 |
| ------------- | ---------------------------------------------------------------------- |
| PDF title     | `BOOKING_RECEIPT_TITLE` = `"Receipt"` (separate from invoice constant) |
| PDF body      | Remove deposit line, amount due, bank transfer block                   |
| PDF body      | Show "Total paid" amount; no Stripe payment ID on customer PDF         |
| Email subject | `Studio booking confirmed - {formatted session date}`                  |
| Email body    | Booking summary + reschedule link; no payment instructions             |
| Host email    | Unchanged (`HostBookingDetailsEmail`)                                  |
| Complete page | Receipt download via `getBookingReceiptPdfByStripeSessionId`           |

`sendBookingReceiptEmailsForBooking` added alongside legacy invoice send (admin/reschedule paths still use invoice). `stripePaymentIntentId` stored on booking for ops/debug; copy UI in stage 4a.

### Frontend (sessions)

Deposit copy removed from `BookingSummary`, `PricingSection`, and `PaymentModal` (implemented). `TermsDialog` cancellation copy still references deposit language — update in stage 5.

<Diff
	id="complete-page-receipt"
	summary="Complete page receipt language"
	filename="src/sites/studio/features/booking-complete/lib/booking-result-content.ts"
	language="ts"
	before={`title: "Your booking is confirmed",
descriptionSteps: [{ title: "Invoice", showInvoiceDownloadLink: true }]`}
	after={`title: "Your booking is confirmed",
descriptionSteps: [{ title: "Receipt", showReceiptDownloadLink: true }]
// copy: "Your receipt has been emailed"`}
/>

### Stage 1 — do not touch yet

- Package invoice-first flow
- Admin payment toggles
- Package adjustment invoices
- `customInvoices` table
- Legacy `paidRemainingBalance` writes (stop new writes in stage 4a)

### Stage 1 — verify

Code complete; manual Stripe test mode QA still needed:

- [ ] Book 1h / 2h / 3h session with addons → Stripe shows itemized total matching form
- [ ] No processing fee line item
- [ ] Webhook confirms → calendar event → receipt email with correct subject
- [ ] Receipt PDF: no bank details, shows "Total paid"
- [ ] `/booking-complete?session_id=...` shows receipt download
- [ ] Abandoned checkout still marks booking `abandoned`
- [ ] Host email still sends

---

## Stage 2 — Packages: Stripe checkout + scheduling email

**Goal:** Packages use the same `PaymentModal` as sessions. On payment, webhook marks paid and emails scheduling link. No admin confirm step.

**Status:** Implemented on `feat/upfront-stripe/package-checkout`. Cron no longer sends package payment reminders; dead reminder query code still in repo (remove in stage 5).

### Backend

<Tabs>
  <Tab id="package-checkout" label="Package checkout action">
    <Code
      language="ts"
      filename="convex/services/packagePayment.ts"
      caption="Replace invoice-first createPackageRequest with checkout session creation"
      code={`// Before: createPendingPackage → sendPackageInvoice → return packageId
// After:  createPendingPackage → createStripeCheckoutSession(package) → return clientSecret

export function createPackageCheckoutSession(...) {
  // 1. Rate limit + email deliverability (existing)
  // 2. createPendingPackage (status: pending_payment)
  // 3. stripe.customers.create → store stripeCustomerId on package
  // 4. buildPackageCheckoutLineItems (duration × packSize, discount line)
  // 5. embedded checkout session, metadata: { packageId }
  // 6. return { clientSecret, stripeSessionId }
}`}
    />
  </Tab>
  <Tab id="package-webhook" label="Webhook handler">
    <Code
      language="ts"
      filename="convex/http.ts"
      caption="New branch for package checkout.session.completed"
      code={`// Detect package vs session from metadata.packageId
// Package path:
//   1. markPackagePaid (paidAt, expiresAt)
//   2. create scheduleTokenHash
//   3. send PackageSchedulingEmail (link ONLY in email)
//   4. status → paid (or schedule_email_failed on send failure)
// No admin confirmPackagePayment step`}
    />
  </Tab>
</Tabs>

**Schema:** add `stripeCustomerId`, `stripeSessionId`, `stripePaymentIntentId` to `packages` if not already present.

**Remove from package create flow:**

- `sendPackageInvoice` on request — done for new packages; keep for legacy resend only
- `invoiceDueAt` writes for new packages — done
- Package payment reminder cron (`packageReminders.ts` payment branch) — cron stopped; delete `listPackagesDueForPaymentReminder*` in stage 5

### Frontend

<Diff
	id="package-submit-checkout"
	summary="Package submit opens PaymentModal instead of redirecting"
	filename="src/sites/studio/features/booking-form/hooks/useBookingSubmit.ts"
	language="ts"
	before={`// package: createPackageRequest → navigate to /package-complete?package_id=...`}
	after={`// package: createPackageCheckoutSession → openPaymentModal(clientSecret)
// on success: navigate to /booking-complete?session_id={CHECKOUT_SESSION_ID}`}
/>

<Diff
	id="unified-complete"
	summary="Packages use same complete page as sessions"
	filename="src/routes/_public/_convex/package-complete.tsx"
	before={`// Separate route, package_id in search params`}
	after={`// Deprecate or redirect to /booking-complete?session_id=...
// Backend query resolves package vs session by stripeSessionId`}
/>

**Complete page (packages):** poll package status by `stripeSessionId`. When paid, show confirmation + "scheduling link emailed" — **no link on page**.

<Code
	language="text"
	caption="Package complete page (after stage 2)"
	code={`┌─────────────────────────────────────┐
│  ✓  4-Session Package confirmed       │
├─────────────────────────────────────┤
│  Payment received.                  │
│  Your receipt has been emailed.     │
│                                     │
│  A link to schedule your sessions   │
│  has been sent to your email.       │
│                                     │
│  [ Download receipt ]               │
└─────────────────────────────────────┘`}
/>

**Abandoned checkout:** mirror session pattern — `closeEmbeddedCheckoutSession` for packages marks package `abandoned`.

### Stage 2 — verify

Code complete; manual Stripe QA + E2E updates deferred to stages 4–5:

- [ ] Package form → PaymentModal → full pack total in Stripe
- [ ] Webhook → paid + scheduling email (no admin action)
- [ ] Complete page polls until paid; no scheduling URL displayed
- [ ] Package payment reminders no longer sent
- [ ] Abandoned package checkout works

---

## Stage 3 — Stripe Invoicing for package adjustments

**Goal:** Remote Podcast overage ($59/session) billed via Stripe Invoice on the package's `stripeCustomerId`. Receipt after `invoice.paid`.

**Status:** Implemented on `feat/upfront-stripe/adjustment-invoicing`. Admin "mark adjustment paid" removed; payment status is read-only in the table.

### Flow

<Mermaid
	source={`sequenceDiagram
  participant App
  participant Stripe
  participant Customer
  App->>App: Package finishes (sessions done or expired)
  App->>Stripe: invoices.create (customerId from package)
  Stripe->>Customer: Payment email (7-day due)
  Customer->>Stripe: Pays invoice
  Stripe->>App: invoice.paid webhook
  App->>Customer: Receipt email (your PDF)`}
/>

### Backend

<Diff
	id="adjustment-stripe-invoice"
	summary="Replace PDF invoice email with Stripe Invoice"
	filename="convex/services/packageAdjustmentInvoices.ts"
	language="ts"
	before={`// Build PDF artifacts → email with attachment → mark invoiceEmailSent`}
	after={`// stripe.invoices.create({
//   customer: package.stripeCustomerId,
//   collection_method: 'send_invoice',
//   days_until_due: 7,
//   metadata: { adjustmentId, packageId },
// })
// stripe.invoiceItems.create per remote session line
// stripe.invoices.finalizeInvoice + sendInvoice
// Store stripeInvoiceId on packageAdjustments`}
/>

**Webhook:** add `invoice.paid` handler in `convex/http.ts`:

- Load adjustment by `stripeInvoiceId` or metadata
- Set `paymentStatus: "paid"`
- Send receipt email (reuse receipt template, adjustment variant)

**Remove:** admin manual "mark adjustment paid" for new adjustments (stage 4a cleans UI).

### Stage 3 — verify

- [ ] Package with remote sessions → Stripe Invoice emailed on completion
- [ ] 7-day due date set
- [ ] `invoice.paid` → adjustment marked paid + receipt email
- [ ] No adjustment invoice when zero remote sessions

---

## Stage 4a — Admin payment cleanup + resend receipt

**Goal:** Remove manual payment tracking for new bookings. Add resend receipt and Stripe Payment ID copy.

**Status:** Open in PR #96 on `feat/upfront-stripe/admin-billing`, stacked on stage 3.

### Remove (new bookings)

| UI / action                     | File(s)                                            | Status                                                  |
| ------------------------------- | -------------------------------------------------- | ------------------------------------------------------- |
| Session paid/unpaid toggle      | `PaymentStatusTabs`, `usePaymentActions`           | Done                                                    |
| Package confirm payment         | admin UI (was `PackageActionDialogs` confirm flow) | Done — delete `confirmPackagePayment` action in stage 5 |
| Adjustment mark paid            | admin package actions                              | Done                                                    |
| Remaining balance override      | `SessionEditDialog`                                | Done                                                    |
| Package payment reminder resend | admin package actions                              | Done (cron stopped)                                     |

### Keep / add

| Feature                                | Detail                                                                                           | Status |
| -------------------------------------- | ------------------------------------------------------------------------------------------------ | ------ |
| Price column in session/package tables | Show full booking total (unchanged display)                                                      | Done   |
| Legacy records                         | Read-only payment fields if present; no edit controls                                            | Done   |
| Resend receipt                         | Regenerate PDF receipt + resend customer email (session + package menus)                         | Done   |
| Stripe IDs (copy)                      | Session/package menu: copy `stripePaymentIntentId` only (intentional — not session/customer IDs) | Done   |

**Stop writing** to: `paidRemainingBalance`, `remainingBalanceAmount` on new session updates.

### Stage 4a — verify

- [ ] New session booking: no payment toggle in admin
- [ ] Old session with `paidRemainingBalance: false`: shows read-only, no toggle
- [ ] Stripe Payment ID copyable from session/package detail menu (`stripePaymentIntentId` only)
- [ ] Resend receipt works for confirmed booking

---

## Stage 4b — Admin Stripe invoicing + receipt download

**Goal:** Replace custom invoice creation with multi-line Stripe Invoice. Keep legacy custom invoices read-only. Add admin receipt PDF download.

**Status:** In progress on `feat/upfront-stripe/admin-stripe-invoicing`, stacked on 4a. Open PR when ready.

### Remove (new bookings)

| UI / action            | File(s)                                             | Status                                                |
| ---------------------- | --------------------------------------------------- | ----------------------------------------------------- |
| Custom invoice dialogs | `CustomInvoiceDialog`, `PackageCustomInvoiceDialog` | Done — `LegacyCustomInvoicesDialog` kept for old rows |

### Keep / add

| Feature                        | Detail                                                                                                           | Status                                                             |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| Receipt PDF download           | Admin download from session/package menus                                                                        | Done                                                               |
| Ad-hoc Stripe Invoice          | Multi-line form: pick booking/package → add rows (description + amount) → one Stripe Invoice with itemized lines | Done — `StripeInvoiceDialog`, `convex/services/stripeInvoicing.ts` |
| Legacy custom invoices         | List/download only when `stripeCustomerId` is missing                                                            | Done                                                               |
| Package-linked session billing | Hide session billing actions for bookings tied to a package                                                      | Done                                                               |
| Legacy resend receipt          | Hide resend receipt when booking has no `stripeCustomerId`                                                       | Done                                                               |

**Backend:** for each line item, `stripe.invoiceItems.create({ customer, invoice, amount, description })`. Customer sees every row on the Stripe-hosted invoice; total is the sum of lines. Implemented in `convex/lib/stripeInvoice.ts` + `convex/services/stripeInvoicing.ts`.

<Code
	language="ts"
	filename="convex/services/stripeInvoicing.ts"
	caption="Ad-hoc invoice creation (sketch)"
	code={`// 1. Load booking/package → stripeCustomerId
// 2. stripe.invoices.create({ customer, collection_method: 'send_invoice', days_until_due: 7 })
// 3. For each admin line: stripe.invoiceItems.create({ customer, invoice, amount, description })
// 4. stripe.invoices.finalizeInvoice + sendInvoice
// Validate: at least one line, all amounts > 0, descriptions non-empty`}
/>

<Code
	language="text"
	caption="Admin ad-hoc charge dialog (multi-line)"
	code={`┌─────────────────────────────────────┐
│  Send Stripe Invoice                │
├─────────────────────────────────────┤
│  Customer: Jane Smith               │
│  Email:    jane@example.com         │
│                                     │
│  Line items                         │
│  ┌───────────────────┬───────────┐  │
│  │ Extra editing hrs │  150.00   │  │
│  │ Rush delivery     │   50.00   │  │
│  └───────────────────┴───────────┘  │
│  [ + Add line item ]                │
│                                     │
│  Total              AUD 200.00    │
│                                     │
│        [ Cancel ]  [ Send invoice ] │
└─────────────────────────────────────┘`}
/>

**Permissions:** `send:receipt-emails` (renamed from `send:invoice-emails`).

**Stop writing** to: `invoiceDueAt` (new records), `customInvoices` (new rows). New packages no longer set `invoiceDueAt`; legacy `invoiceEmailStatus: "pending"` on insert can go in stage 5.

### Stage 4b — remaining

- [ ] Open PR for `feat/upfront-stripe/admin-stripe-invoicing` stacked on 4a
- [x] Renamed `send:invoice-emails` → `send:receipt-emails` in `src/lib/permissions.ts` and Convex callers
- [ ] Run stage 4b verify checklist below

### Stage 4b — verify

- [ ] Admin ad-hoc Stripe Invoice with multiple line items sends; customer sees itemized total
- [ ] Single-line invoice still works (one row)
- [ ] Receipt PDF download works for session and package
- [ ] Legacy custom invoice list/download only when `stripeCustomerId` is missing
- [ ] Package-linked session: no session billing actions in admin menu
- [ ] Legacy booking without `stripeCustomerId`: no resend receipt action

---

## Stage 5 — Tests, cleanup, copy

**Goal:** Confidence + remove dead code paths. No behavior changes.

Ship as stacked parts on `feat/upfront-stripe/tests-cleanup`. Each part is one commit (or one small PR slice). Do them in order.

<Callout
	tone="decision"
	body="**Legacy invoice PDFs stay.** Admin still downloads and emails invoice PDFs for old sessions/packages (`stripeCustomerId` missing). Do not remove `calculate-booking-invoice-amounts`, deposit constants, `includeDepositLineItem`, or legacy custom-invoice helpers. Stage 5 only stops new customer flows from using invoice/deposit copy."
/>

### Part 1 — E2E (CI pre-checkout + local full checkout)

**Status:** Done — CI runs `book.spec.ts` + `home.spec.ts`; local `package-payment.spec.ts` covers checkout through session 1 scheduling (replaces deleted `package-schedule.spec.ts`).

Packages now behave like single sessions: form → terms → Stripe payment modal → `/booking-complete?session_id=...`. Stripe Pay + hCaptcha blocks headless checkout in GitHub Actions, so **full checkout E2E is local only**. CI stays pre-checkout (form + payment modal, no pay). No post-checkout E2E in CI.

**CI (`bun run test:e2e`, runs in GitHub Actions)**

| Spec                           | Change                                                                                                                                                                                            |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `e2e/book.spec.ts`             | **Session:** unchanged — fill form, agree to terms, assert payment modal opens, close in `finally`.                                                                                               |
| `e2e/book.spec.ts`             | **Package:** replace `/package-complete` + "no payment modal" with the same pattern as session — open payment modal, assert Stripe UI, close in `finally`. Remove `expectPackageRequestComplete`. |
| `e2e/home.spec.ts`             | Unchanged.                                                                                                                                                                                        |
| `e2e/package-schedule.spec.ts` | **Remove from CI** — delete from `test:e2e` script; spec still uses admin confirm payment (removed in stage 4a).                                                                                  |

**Local only (Stripe test keys in `.env.local`, not run in CI)**

| Spec                             | Purpose                                                                                                                                                           |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `e2e/session-payment.spec.ts`    | Session: pay test card → `/booking-complete` confirmed. Already exists.                                                                                           |
| `e2e/package-payment.spec.ts`    | **New.** Package: pay test card → `/booking-complete` → poll Resend for `/package-schedule/...` → schedule session 1.                                             |
| `e2e/package-schedule.spec.ts`   | **Rewrite** as local-only (or fold into `package-payment.spec.ts`). Drop `confirmPackagePaymentForCustomer`; start from Stripe checkout instead of admin confirm. |
| `e2e/session-reschedule.spec.ts` | Session: pay → receipt email → reschedule UI. Already local-only.                                                                                                 |

**Scripts / helpers**

- `package.json`: `test:e2e` = `book.spec.ts` + `home.spec.ts` only. Add `test:e2e:package-payment` (and keep `test:e2e:session-reschedule`, `test:e2e:session-payment` if split).
- `e2e/helpers/booking-form.ts`: reuse `expectPaymentModal`, `completeStripePayment`, `expectBookingConfirmed` for packages; remove `/package-complete` helpers for new flow.
- `e2e/helpers/admin-packages.ts`: remove `confirmPackagePaymentForCustomer` (dead after stage 4a).

**Convex tests (already in CI via `bun run test`)**

| Area                                                   | Status                                                                                          |
| ------------------------------------------------------ | ----------------------------------------------------------------------------------------------- |
| Checkout line item amounts                             | Done — `convex/lib/tests/stripeCheckoutLineItems.test.ts` (tautology fix applied)               |
| Webhook idempotency (checkout replay)                  | Done — `convex/tests/stripeCompletion.test.ts`, `convex/lib/tests/packageCheckoutClaim.test.ts` |
| Package paid → schedule token once                     | Partial — extend only if a real gap appears after Part 2                                        |
| Adjustment invoice guards + `invoice.paid` idempotency | Done — `convex/tests/adjustmentInvoices.test.ts`                                                |
| Reminder timezone math                                 | Done — removed tautological wrapper test in `reminderScheduleTime.test.ts`                      |

### Part 2 — Backend dead code

**Status:** Done — `confirmPackagePayment`, `listPackagesDueForPaymentReminder*`, create-path `sendPackageInvoice`, and package `invoiceEmailStatus: "pending"` insert removed.

Remove paths made obsolete by stages 1–4. Grep after each deletion.

- Delete `confirmPackagePayment` action/handler
- Delete `listPackagesDueForPaymentReminder*` (cron stopped in stage 2)
- Remove unused `sendPackageInvoice` on the **create** path if fully legacy-only
- Remove `invoiceEmailStatus: "pending"` from `buildPendingPackageRecord` when safe

### Part 3 — Customer-facing copy

**Status:** Done — `TermsDialog` + `/terms-and-conditions` use upfront payment and non-refundable cancellation copy; no deposit/bank-transfer/PayID on `/book`, complete pages, or receipt emails.

Update copy on **new** booking and complete flows only. Do not change legacy admin invoice/receipt menus.

- `TermsDialog` and `/terms-and-conditions`: full upfront payment non-refundable, rescheduling with notice
- Grep: `"bank transfer"`, `"PayID"`, `"deposit"` should not appear on `/book`, complete pages, or customer emails for new checkout. Hits in legacy admin invoice PDFs and `stripeCustomerId === undefined` menus are expected.

Receipt paths already use `BOOKING_RECEIPT_TITLE`, `BOOKING_RECEIPT_NOTES`, and `calculate-booking-receipt-amounts` (no deposit line). Part 3 is mostly Terms + any stray customer copy, not ripping out invoice constants.

### Part 4 — Legacy invoice PDF boundary (audit, not removal)

**Status:** Done — receipt/checkout paths use `BOOKING_RECEIPT_*` only; package receipt cancellation policy moved off `BOOKING_INVOICE_NOTES`. Legacy invoice libs unchanged.

**Keep** for admin legacy sessions/packages:

- `buildBookingInvoiceData`, `calculate-booking-invoice-amounts`, `BOOKING_DEPOSIT_AMOUNT`, `BOOKING_INVOICE_PAYMENT`, `includeDepositLineItem`
- `legacy-custom-invoices.ts`, `LegacyCustomInvoicesDialog`, `downloadAdminBookingInvoice`, `sendBookingInvoiceForBooking`

**Do not** remove `includeDepositLineItem` from custom-invoice render paths. Stored rows may still have `includeDepositLineItem: true`.

**Audit only:** confirm new receipt/checkout code does not import deposit or bank-transfer constants. Leave invoice libs frozen unless fixing a bug.

### Part 5 — Verify gate

Run before merge:

- [ ] `bun run format && bun run lint && bun run test && bun run typecheck`
- [ ] No references to `STRIPE_BOOKING_DEPOSIT_PRICE_ID` or `STRIPE_PROCESSING_FEE_PRICE_ID`
- [ ] CI E2E: session + package both stop at payment modal (no Stripe pay)
- [ ] Local E2E (optional): `session-payment`, `package-payment`, `session-reschedule`
- [ ] Grep customer-facing copy clean; legacy admin invoice paths still work for old records

---

## PR checklist (every stage)

<Callout
	tone="success"
	body="Before `gh stack submit` or merging a stage PR: format, lint, typecheck, test. E2E for stages 1–2 if checkout UI changed."
/>

1. Changes are on the correct stack branch (see stages table)
2. Stage is independently deployable (or clearly gated — e.g. stage 2 before enabling package checkout in prod)
3. No new writes to deprecated fields (stage 4+)
4. Webhook handlers are idempotent
5. Stripe metadata links records (`bookingId`, `packageId`, `adjustmentId`)
6. Customer emails never ask for payment after stage 1/2 checkout

---

## Out of scope

- Migrating old unpaid bank-transfer bookings (none pending per design review)
- Stripe Customer reuse by email (new customer per checkout)
- Scheduling link on complete page (email only)
- Baking Stripe fees into displayed prices (studio absorbs fees)
- Formal AU tax invoice with GST breakdown (receipt with ABN is sufficient)
